using UnityEngine;
using System.Collections;

public class CollisionsSettings : MonoBehaviour {

    public bool right=true, left=true, up=true, down=true;
	// Use this for initialization
	void Start () {
	
	}
	
	// Update is called once per frame
	void Update () {
	
	}
}
